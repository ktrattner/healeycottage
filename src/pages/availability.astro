---
import Base from "../layouts/Base.astro";
import rooms from "../content/rooms.json";
---
<Base title="Availability">
  <h1>Availability</h1>
  {rooms.map(r => (
    <>
      <h2>{r.name}</h2>
      <iframe src={r.calendarEmbedUrl} width="100%" height="500"></iframe>
    </>
  ))}
</Base>